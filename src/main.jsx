import React from 'react'
import ReactDOM from 'react-dom/client'
import GovConPipeline from './GovConPipelineTracker'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GovConPipeline />
  </React.StrictMode>
)
