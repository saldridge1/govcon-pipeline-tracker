import React from 'react'
import ReactDOM from 'react-dom/client'
import GovConReady from './GovConReady'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GovConReady />
  </React.StrictMode>
)
